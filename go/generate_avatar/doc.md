# 头像生成

生成png图片，长宽比为1:1（具体像素数参考输入的尺寸等级），左右对称；图片主体为紧密排列的5*5方格，外围有一圈白边。

- 每个方格显示指定颜色（根据输入字符串计算，详见实现小节）或白色
- 假设每一个小方格边长为`2a`，则外围的白边宽度为`a`
- 图片基础长宽像素数为`12*12`（取a=1），用户输入的尺寸等级作为倍数，乘在基础像素上
   - 举个例子，假设尺寸等级为4，则最终图片像素数为`48*48`
- 为什么外围有一圈白边：可以方便剪裁为圆形而保留基本样式

示例图片：

![img](img/mario_7.png)
![img](img/Mario_5.png)
![img](img/mats0319_6.png)

## 规则

1. 输入一个**字符串**和想要的图片**尺寸等级**
2. 根据输入字符串计算hash，根据hash计算图像使用的**颜色**，以及**在哪些位置显示色块**
3. 将上一步中的计算结果编码成图片格式，写成文件

## 使用

> 参考test代码

如果你需要修改参数或go版本，可以将`.go`文件复制到你的项目

## 实现

1. 根据输入字符串计算hash
    - hash algorithm：`sha256`
    - 结果类型：16进制字符串（长度：64）
2. `64 = 15 * 4 + 4`：将hash分为16组，前15组表示对应位置是否显示色块，最后一组值用来表示图像颜色
    - 图片左右对称，15组信息用来表示左侧两列与中间列的方格是否显示色块
    - 一个小方格显示色块的条件：该组最后一个bit为1
        - 每组4位16进制字符、16个bit，如果最后一个bit为1则显示色块、为0则显示白块
        - 每组的前15个bit实际上没有用到
    - 色块的颜色：假设最后一组hash为`0xabcd`，则色块颜色为：`RGBA(ab, bc, cd, da)`
3. 记录以上计算结果，使用`image`包编码图片信息，序列化并写入文件
